# This folder moved

The lab Terraform root is now `stacks/roadmap`.

```bash
cd stacks/roadmap
terraform init
terraform plan
```

Do not add `.tf` files here again. One stack lives in one folder so you never apply the same site twice.
